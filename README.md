# Breast_Cancer_Classification
Lightweight CNN model for breast cancer classification using gene expression data from the Pan-Cancer Atlas (Illumina HiSeq). Gene data is preprocessed (outlier removal via AAIC, normalization, and filtering) and transformed into 2D images. Hyperparameter tuning yields a 0.92% accuracy.
